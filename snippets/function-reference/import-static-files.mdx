<ParamField body="context.utils.importStaticFiles" >

  <Expandable title="function parameters">

    <ParamField body="source" type="string">
      The path to the source folder with the static files to be copied. 
      For example,
      
      ```typescript
        const staticPath = resolve(__dirname, "./static");
      ```
    </ParamField>

    <ParamField body="basePath" type="string">
    The base path of the target folder where the static files will be copied.
    </ParamField>
    
  </Expandable >
</ParamField>